# moved

tries to be idempotent.

excluded:

- network settings tweaks are excluded as devices use different network drivers.
- widgets, pinned start menu items, pinned taskbar items, system trays, search web suggestions

# removed-settings

- set `Sleep` -> `Sleep after` -> `On battery (Minutes)` to `0` (can be modified in windows settings)
- set `Sleep` -> `Sleep after` -> `Plugged in (Minutes)` to `0` (can be modified in windows settings)

- set `Display` -> `Turn off display after` -> `On battery (Minutes)` to `0` (can be modified in windows settings)
- set `Display` -> `Turn off display after` -> `Plugged in (Minutes)` to `0` (can be modified in windows settings)

# browsers

google

- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Google Password Manager` -> `Google Password Manager` -> `Settings` -> `Settings` -> `Offer to save passwords and passkeys`
- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Payment methods` -> `Payment methods` -> `Save and fill payment methods`
- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Addresses and more` -> `Addresses and more` -> `Save and fill addresses`

edge

- turn off `Passwords and autofill` -> `Passwords and autofill` -> `Microsoft Password Manager` -> `Ask to save passwords and passkeys`
- turn off `Passwords and autofill` -> `Passwords and autofill` -> `Payment methods` -> `Payment methods` -> `Save and fill payment methods`
- turn off `Passwords and autofill` -> `Passwords and autofill` -> `Addresses and more` -> `Addresses and more` -> `Save and autofill addresses`

firefox

- turn off `Privacy and Security` -> `Passwords` -> `Ask to save passwords`
- turn off `Privacy and Security` -> `Payment methods` -> `Save and autofill payment info`
- turn off `Privacy and Security` -> `Addresses and more` -> `Save and autofill addresses` (skip if hidden)

opera gx

- turn off `AUTOFILL AND PASSWORDS` -> `AUTOFILL AND PASSWORDS` -> `Password Manager` -> `Password Manager` -> `Settings` -> `Offer to save passwords`
- turn off `AUTOFILL AND PASSWORDS` -> `AUTOFILL AND PASSWORDS` -> `Payment methods` -> `PAYMENT METHODS` -> `Save and fill payment methods`
  save passwords`
- turn off `AUTOFILL AND PASSWORDS` -> `AUTOFILL AND PASSWORDS` -> `Addresses and more` -> `ADDRESSES AND MORE` -> `Save and fill addresses`

brave

- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Password Manager` -> `Settings` -> `Settings` -> `Offer to save passwords and passkeys`
- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Payment methods` -> `Payment methods` -> `Save and fill payment methods`
- turn off `Autofill and passwords` -> `Autofill and passwords` -> `Addresses and more` -> `Addresses and more` -> `Save and fill addresses`
